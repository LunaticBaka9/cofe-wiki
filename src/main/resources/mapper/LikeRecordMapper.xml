<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd"
>

<mapper namespace="com.maid_coffee.mapper.LikeRecordMapper">
    <resultMap id="BaseResultMap" type="com.maid_coffee.entity.LikeRecord">
        <id column="id" property="id"/>
        <result column="comment_id" property="commentId"/>
        <result column="user_id" property="userId"/>
        <result column="created_date" property="createdDate"/>
    </resultMap>

    <select id="selectByCommentIdAndUserId" resultMap="BaseResultMap">
        SELECT * FROM like_record
        WHERE comment_id = #{commentId} AND user_id = #{userId}
        LIMIT 1
    </select>

    <select id="selectByCommentIdsAndUserId" resultMap="BaseResultMap">
        SELECT * FROM like_record
        WHERE comment_id IN
        <foreach collection="commentIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND user_id = #{userId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO like_record (comment_id, user_id, created_date)
        VALUES (#{commentId}, #{userId}, NOW())
    </insert>

    <delete id="deleteByCommentIdAndUserId">
        DELETE FROM like_record
        WHERE comment_id = #{commentId} AND user_id = #{userId}
    </delete>
</mapper>
