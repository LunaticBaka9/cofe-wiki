<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd"
>

<mapper namespace="com.maid_coffee.mapper.RoleMapper">

    <select id="selectAllRoles" resultType="com.maid_coffee.entity.Role">
        select * from `role`
        <where>
            <if test="roleName != null and roleName != ''">
                roleName like concat('%', #{roleName}, '%')
            </if>
            <if test="roleCode != null and roleCode != ''">
                and roleCode like concat('%', #{roleCode}, '%')
            </if>
        </where>
    </select>

    <select id="selectById" resultType="com.maid_coffee.entity.Role">
        select * from `role` where roleId = #{roleId}
    </select>

    <select id="selectByRoleCode" resultType="com.maid_coffee.entity.Role">
        select * from `role` where roleCode = #{roleCode}
    </select>

    <insert id="insert">
        insert into `role` (roleName, roleCode, description, createTime, updateTime)
        values(#{roleName}, #{roleCode}, #{description}, NOW(), NOW())
    </insert>

    <update id="update">
        update `role` set roleName=#{roleName}, roleCode=#{roleCode},
        description=#{description}, updateTime=NOW()
        where roleId = #{roleId}
    </update>

    <delete id="delete">
        delete from `role` where roleId = #{roleId}
    </delete>
</mapper>
